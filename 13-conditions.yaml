- name: check a number less than 10 or not
  hosts: local
  connection: local
  vars:
   NUMBER_OF_SESSIONS: 7
  tasks:
   - name: Is ansible completed more than 5 NUMBER_OF_SESSIONS
     ansible.builtin.debug:
      msg: "Yes more than 5 sessions"
     when: NUMBER_OF_SESSIONS > 5